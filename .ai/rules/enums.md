---
paths:
  - 'app/Enums/**'
---

# Enums

## Enums live in app/Enums
All enums belong in app/Enums (App\Enums namespace) — make:enum defaults to app/, so pass the path or move the file. Permission is the pattern: string-backed, TitleCase cases, kebab-case values.
